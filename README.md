# Vibeshop

A local-first, GPU-native photo editor written in Rust.

The first implementation is being built on `agent/bootstrap-editor`. The target is a complete import → edit → export loop, not a mockup or a Photoshop-parity claim.
